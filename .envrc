if ! has nix; then
  echo "Error: nix is not installed. Install nix first: https://nixos.org/download.html"
  exit 1
fi

use flake

# Also add node_modules/.bin to PATH for local tools
PATH_add node_modules/.bin
